name: Generate ASCII Artwork

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: List Final Files
        run: ls -l

      - name: Execute script
        run: ./script.sh
      - name: List Final Files
        run: ls -l

      